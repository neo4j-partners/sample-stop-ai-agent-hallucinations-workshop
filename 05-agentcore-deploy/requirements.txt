# Lab 5 deploys what Lab 4 built, so it needs the same shared package.
-e ../workshop

bedrock-agentcore-starter-toolkit==0.3.11
# Upper bound on purpose. booking_agent.py builds on the hook API, and
# BeforeToolCallEvent and AfterToolCallEvent are what pin the caller's
# request_id and carry the command's verdict back out. A major version is free
# to rename either one, so let the resolver take 1.x only.
strands-agents==1.52.0

# deployment-tools/booking_agent.py is the file that ships into the image, but a
# facilitator reads and edits it with this venv active. Declaring its two
# remaining imports here keeps them resolved locally. They are pinned for the
# image separately, in deployment-tools/agent_requirements.txt.
bedrock-agentcore==1.21.0
mcp==1.29.0
